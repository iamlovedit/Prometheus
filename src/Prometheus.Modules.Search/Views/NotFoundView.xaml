<UserControl
    x:Class="Prometheus.Modules.Search.Views.NotFoundView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="72"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Border
        Padding="14,11"
        Background="{DynamicResource MainContentForegroundDrawingBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="36" /><ColumnDefinition Width="12" /><ColumnDefinition /></Grid.ColumnDefinitions>
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="{StaticResource Iconfont}"
                FontSize="28"
                FontWeight="Bold"
                Foreground="{DynamicResource WarningBrush}"
                Text="&#xe688;" />
            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                <TextBlock FontWeight="SemiBold" Text="{DynamicResource Search.UserNotFound}" />
                <TextBlock Margin="0,3,0,0" Style="{DynamicResource PageCaption}" Text="{DynamicResource Search.NotFound.Description}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
