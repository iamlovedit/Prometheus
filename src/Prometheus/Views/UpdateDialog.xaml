<UserControl
    x:Class="Prometheus.Views.UpdateDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:Prometheus.ViewModels"
    Width="560"
    d:DataContext="{d:DesignInstance Type=viewmodels:UpdateDialogViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="{DynamicResource Update.Dialog.Title}" />
            <TextBlock Margin="0,5,0,0" Foreground="{DynamicResource SecondaryTextBrush}">
                <Run Text="{DynamicResource Update.Dialog.Version}" />
                <Run Text=" " />
                <Run FontWeight="SemiBold" Text="{Binding Version}" />
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="2" Padding="12" Background="{DynamicResource SecondaryRegionBrush}" CornerRadius="8">
            <TextBlock Text="{Binding ReleaseNotes}" TextWrapping="Wrap" />
        </Border>

        <StackPanel Grid.Row="4">
            <TextBlock Text="{Binding StatusText}" />
            <ProgressBar Height="7" Margin="0,8,0,0" Maximum="100" Value="{Binding Progress}" />
        </StackPanel>

        <TextBlock
            Grid.Row="6"
            Foreground="{DynamicResource DangerBrush}"
            Text="{Binding ErrorMessage}"
            TextWrapping="Wrap" />

        <StackPanel Grid.Row="8" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="0,0,8,0" Command="{Binding LaterCommand}" Content="{DynamicResource Update.Dialog.Later}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMandatory}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Margin="0,0,8,0" Command="{Binding ExitCommand}" Content="{DynamicResource Update.Dialog.Exit}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMandatory}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button
                Command="{Binding InstallCommand}"
                Content="{DynamicResource Update.Dialog.Install}"
                Style="{DynamicResource ButtonPrimary}" />
        </StackPanel>
    </Grid>
</UserControl>
