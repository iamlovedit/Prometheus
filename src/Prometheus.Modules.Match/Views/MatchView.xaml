<UserControl
    x:Class="Prometheus.Modules.Match.Views.MatchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:Prometheus.Modules.Match.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:Prometheus.Modules.Match.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MatchViewModel}"
    d:DesignHeight="760"
    d:DesignWidth="1080"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="76" />
            <RowDefinition Height="12" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <components:MatchHeaderView />
        <Grid Grid.Row="2">
            <components:LiveMatchRosterView Visibility="{Binding HasRoster, Converter={StaticResource Boolean2VisibilityConverter}}" />
            <components:PostGameView Visibility="{Binding IsPostGameVisible, Converter={StaticResource Boolean2VisibilityConverter}}" />
            <components:MatchEmptyStateView Visibility="{Binding ShowEmptyState, Converter={StaticResource Boolean2VisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
